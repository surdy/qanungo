[package]
name = "qanungo"
description = "Read-side coaching client over the Patwari archive"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
chrono.workspace = true
clap.workspace = true
munshi-transcript.workspace = true
rustls.workspace = true
rustls-native-certs.workspace = true
serde_json.workspace = true
sha2.workspace = true
thiserror.workspace = true
zstd.workspace = true

[dev-dependencies]
tempfile.workspace = true
# The stand-in archive in `tests/mirror.rs` serves zstd-compressed artifact bytes, exactly as
# Patwari does, so the client's decode path is exercised rather than assumed.
zstd.workspace = true
